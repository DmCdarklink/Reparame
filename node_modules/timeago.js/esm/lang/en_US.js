var EN_US = ['segundo', 'minuto', 'hora', 'dia', 'semana', 'mes', 'año'];
export default function (diff, idx) {
    if (idx === 0)
        return ['justo ahora', 'ahora'];
    var unit = EN_US[Math.floor(idx / 2)];
    if (diff > 1)
        unit += 's';
    return [diff + " " + unit + " atrás", "in " + diff + " " + unit];
}
//# sourceMappingURL=en_US.js.map